# Core application
numpy>=1.24.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0  # PostgreSQL adapter
alembic>=1.12.0         # Database migrations

# Data processing
openpyxl>=3.1.0        # Excel file reading (legacy)
polars>=0.20.0         # Fast DataFrame library with fastexcel support
fastexcel>=0.10.0      # Rust-based Excel reader (10-100x faster than pandas)

# Development and testing
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0           # Code formatting
flake8>=6.0.0           # Linting

# Additional utilities
python-dotenv>=1.0.0    # Environment variable management
click>=8.0.0            # CLI tool creation
tqdm>=4.65.0            # Progress bars for data loading
requests>=2.31.0        # HTTP requests for data downloads

# Azure deployment (003-hosted-sql-db)
azure-identity==1.15.0
azure-mgmt-sql
azure-mgmt-resource==23.0.0
azure-mgmt-authorization>=1.0.0
pyodbc>=5.0.0
pymssql>=2.2.0